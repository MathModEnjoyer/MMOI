# Лабораторная работа №4. Обнаружение объектов на изображении

## 1. Цель работы

Изучить и экспериментально исследовать методы обнаружения объектов на изображениях при помощи линейной обработки «окном».

В ходе лабораторной работы необходимо познакомиться с:

- корреляционным методом обнаружения объектов;
- использованием эталона объекта;
- согласованной фильтрацией;
- разбеливанием коррелированного фона;
- пороговой обработкой корреляционных пиков;
- оценкой качества обнаружения по количеству ложных и пропущенных объектов.

# 2. Теоретический минимум

## 2.1. Задача обнаружения объектов

Задача обнаружения заключается в установлении наличия на изображении объектов с определёнными свойствами и, если объекты обнаружены, в определении их координат.

Основной принцип состоит в сравнении фрагментов исследуемого изображения с некоторым **эталоном** — изображением искомого объекта.

Пусть:

- $t(k,l)$ — функция яркости эталонного объекта;
- $D$ — область определения эталона;
- $x(m,n)$ — исследуемое изображение.

Эталон последовательно перемещается по изображению. В каждом положении определяется степень его сходства с соответствующим фрагментом изображения.

## 2.2. Квадратичная мера различия

Одной из мер различия изображения и эталона является

$$
\varepsilon^2(m,n)
=
\sum_{(k,l)\in D}
\left[
x(m+k,n+l)-t(k,l)
\right]^2.
$$

Фрагмент изображения считается похожим на эталон, если

$$
\varepsilon^2(m,n)<L_\varepsilon,
$$

где $L_\varepsilon$ — заданный порог.

На практике непосредственно эту величину обычно не вычисляют. Для обнаружения объектов используется взаимная корреляция изображения и эталона.

# 3. Корреляционный метод обнаружения

Взаимная корреляция изображения с эталоном определяется как

$$
B(m,n)
=
\sum_{(k,l)\in D}
x(m+k,n+l)t(k,l).
$$

При хорошем совпадении изображения с эталоном значение $B(m,n)$ увеличивается.

Однако величина взаимной корреляции зависит не только от сходства с эталоном, но и от уровня яркости изображения.

Для уменьшения этого влияния может использоваться нормированная взаимная корреляция:

$$
R(m,n)
=
\frac{
\displaystyle
\sum_{(k,l)\in D}
x(m+k,n+l)t(k,l)
}{
\sqrt{
\displaystyle
\sum_{(k,l)\in D}
x^2(m+k,n+l)
}
\sqrt{
\displaystyle
\sum_{(k,l)\in D}
t^2(k,l)
}
}.
$$

Если изображение и эталон совпадают с точностью до постоянного неотрицательного множителя, то

$$
R(m,n)=1.
$$

Объект считается обнаруженным, если

$$
R(m,n)>L_R,
$$

где $L_R$ — порог обнаружения.

## 3.1. Реализация коррелятора

Корреляционная обработка может быть реализована как линейная обработка изображения с маской, соответствующей эталону, повёрнутому на $180^\circ$:

$$
h(k,l)=t(-k,-l).
$$

После обработки получается изображение корреляционных пиков.

Высокие значения на таком изображении соответствуют участкам, похожим на искомый объект.

После этого выполняется пороговая обработка:

$$
g(m,n)=
\begin{cases}
0, & B(m,n)\le L_R,\\
1, & B(m,n)>L_R.
\end{cases}
$$

# 4. Метод согласованной фильтрации

Корреляционный метод учитывает форму и распределение яркости объекта, но не учитывает свойства фона.

Согласованная фильтрация позволяет учитывать статистические характеристики фона.

Рассматривается модель, в которой наблюдаемое изображение состоит либо только из фона

$$
x(m,n)=f(m,n),
$$

либо из объекта и фона

$$
x(m,n)=t(m,n)+f(m,n),
$$

где:

- $t(m,n)$ — искомый объект;
- $f(m,n)$ — случайный фон.

Согласованный фильтр выбирается таким образом, чтобы увеличить превышение сигнала объекта над фоном.

Критерий имеет вид

$$
\delta^2
=
\frac{
y_t^2(0,0)
}{
D_{y_f}
}
\rightarrow \max,
$$

где:

- $y_t(0,0)$ — отклик фильтра на объект;
- $D_{y_f}$ — дисперсия фона на выходе фильтра.

В частотной области согласованный фильтр определяется выражением

$$
H(e^{i\omega_1},e^{i\omega_2})
=
\frac{
T(e^{-i\omega_1},e^{-i\omega_2})
}{
\Phi_f(e^{i\omega_1},e^{i\omega_2})
},
$$

где:

- $T$ — спектр эталона;
- $\Phi_f$ — энергетический спектр фона.

Если фон представляет собой белый шум, согласованный фильтр с точностью до постоянного множителя совпадает с коррелятором.

Если фон коррелирован, перед корреляционной обработкой необходимо выполнить его разбеливание.

# 5. Разбеливание фона

Согласованный фильтр можно условно представить как последовательность двух операций:

1. разбеливание фона;
2. корреляционная обработка разбеленного изображения.

Разбеливающий фильтр преобразует коррелированный фон в фон, близкий по свойствам к белому шуму.

После разбеливания производится корреляция с эталоном.

# 6. Фон с биэкспоненциальной корреляционной функцией

В лабораторной работе фон моделируется случайным полем с биэкспоненциальной корреляционной функцией

$$
B_f(m,n)
=
D_f\rho^{\lvert m\rvert+\lvert n\rvert},
$$

где:

- $D_f$ — дисперсия фона;
- $\rho$ — коэффициент корреляции между соседними отсчётами.

Для такого фона разбеливание можно выполнить обработкой изображения окном $3\times3$ с маской

$$
\begin{pmatrix}
a_2 & a_1 & a_2\\
a_1 & 1 & a_1\\
a_2 & a_1 & a_2
\end{pmatrix},
$$

где

$$
a_0=
\left(
\frac{1+\rho^2}{1-\rho^2}
\right)^2,
$$

$$
a_1=
-\frac{\rho}{1+\rho^2},
$$

$$
a_2=
\frac{\rho^2}{(1+\rho^2)^2}.
$$

Коэффициент $a_0/D_f$ является масштабным множителем и при обработке может не учитываться, поскольку влияет только на масштаб значений и последующий выбор порога.

Для используемого во втором варианте значения

$$
\rho=0.8
$$

коэффициенты имеют значения

$$
a_0=20.75,
$$

$$
a_1=-0.4878,
$$

$$
a_2=0.2380.
$$

# 7. Эталоны объектов

В работе используются объекты размером

$$
3\times3.
$$

Тип объекта задаётся преподавателем.

### Объект `x`

$$
\begin{pmatrix}
1 & 0 & 1\\
0 & 1 & 0\\
1 & 0 & 1
\end{pmatrix}
$$

### Объект `+`

$$
\begin{pmatrix}
0 & 1 & 0\\
1 & 1 & 1\\
0 & 1 & 0
\end{pmatrix}
$$

### Объект `T`

$$
\begin{pmatrix}
1 & 1 & 1\\
0 & 1 & 0\\
0 & 1 & 0
\end{pmatrix}
$$

### Объект `□`

$$
\begin{pmatrix}
1 & 1 & 1\\
1 & 0 & 1\\
1 & 1 & 1
\end{pmatrix}
$$

### Объект `Ⱶ`

$$
\begin{pmatrix}
1 & 0 & 0\\
1 & 1 & 1\\
1 & 0 & 0
\end{pmatrix}
$$`

# 8. Общая схема исследования

1. Сгенерировать изображение фона размером

$$
64\times64.
$$

2. Фон должен иметь:

   - нулевое среднее;
   - заданную дисперсию $D_f$;
   - заданный коэффициент корреляции $\rho$.

3. Сгенерировать отдельное изображение размером

$$
64\times64,
$$

содержащее набор объектов заданного типа.

4. Размер каждого объекта:

$$
3\times3.
$$

5. Получить исследуемое изображение суммированием изображения фона и изображения объектов.

6. Объекты не должны пересекаться.

7. Для корреляционного метода выполнить обработку изображения коррелятором.

8. Для согласованной фильтрации:

   - выполнить разбеливание фона;
   - выполнить обработку коррелятором.

9. Для полученного изображения корреляционных пиков:

   - построить гистограмму;
   - визуализировать её;
   - выбрать порог;
   - выполнить пороговую обработку.

10. По итоговому изображению определить:

   - число правильно обнаруженных объектов;
   - число ложных обнаружений;
   - число пропущенных объектов.

Если требуется обнаруживать несколько типов объектов, корреляционная и пороговая обработка выполняются отдельно для каждого типа.

# 9. Варианты лабораторной работы

Каждый студент выполняет вариант, назначенный преподавателем.

## 9.1. Вариант 1. Корреляционный метод

Для заданного преподавателем типа объекта исследовать корреляционный метод обнаружения при некоррелированном фоне.

### Параметры

Количество объектов:

$$
N=4.
$$

Коэффициент корреляции фона:

$$
\rho=0.
$$

Исследование провести для двух значений дисперсии фона:

$$
D_f=3000,
$$

$$
D_f=5000.
$$

### Обязательные задания

1. Сгенерировать фон размером $64\times64$ с нулевым средним.

2. Сгенерировать четыре объекта заданного преподавателем типа.

3. Разместить объекты так, чтобы они не пересекались.

4. Сформировать исследуемое изображение как сумму фона и изображения объектов.

5. Реализовать корреляционную обработку с эталоном объекта.

6. Получить изображение корреляционных пиков.

7. Построить его гистограмму.

8. Выбрать порог обнаружения.

9. Выполнить пороговую обработку.

10. Определить:
    - число ложных обнаружений;
    - число пропущенных объектов.

11. Выполнить эксперимент для

$$
D_f=3000.
$$

12. Повторить эксперимент для

$$
D_f=5000.
$$

## 9.2. Вариант 2. Сравнение корреляционного метода и согласованной фильтрации

Для двух типов объектов, заданных преподавателем, сравнить:

- корреляционный метод;
- метод согласованной фильтрации.

### Параметры

Количество объектов каждого типа:

$$
N=2.
$$

Дисперсия фона:

$$
D_f=3000.
$$

Коэффициент корреляции:

$$
\rho=0.8.
$$

### Обязательные задания

1. Сгенерировать фон размером $64\times64$ с заданными $D_f$ и $\rho$.

2. Сгенерировать по два объекта каждого из двух заданных типов.

3. Разместить объекты без пересечений.

4. Сформировать исследуемое изображение.

5. Реализовать корреляционный метод обнаружения для первого типа объектов.

6. Построить изображение корреляционных пиков.

7. Построить гистограмму и выбрать порог.

8. Выполнить пороговую обработку.

9. Повторить обработку для второго типа объектов.

10. Определить число ложных и пропущенных объектов для корреляционного метода.

11. Реализовать разбеливание фона с помощью маски

$$
\begin{pmatrix}
a_2 & a_1 & a_2\\
a_1 & 1 & a_1\\
a_2 & a_1 & a_2
\end{pmatrix}.
$$

12. После разбеливания выполнить корреляционную обработку для первого типа объектов.

13. Построить гистограмму корреляционных пиков и выбрать порог.

14. Выполнить пороговую обработку.

15. Повторить обработку для второго типа объектов.

16. Определить число ложных и пропущенных объектов для метода согласованной фильтрации.

17. Сравнить результаты двух методов.

# 10. Эксперименты

## 10.1. Вариант 1

Сравнить качество обнаружения при

$$
D_f=3000
$$

и

$$
D_f=5000.
$$

Для каждого случая определить:

- число ложных обнаружений;
- число пропущенных объектов.

Объяснить влияние изменения дисперсии фона на полученный результат.

## 10.2. Вариант 2

Для каждого из двух типов объектов сравнить:

- корреляционный метод;
- согласованную фильтрацию.

Для каждого метода определить:

- число ложных обнаружений;
- число пропущенных объектов.

Объяснить наблюдаемую разницу между методами.

# 11. Результаты

Для своего варианта необходимо представить основные этапы обработки.

### Вариант 1

- изображение фона;
- изображение объектов;
- результирующее изображение «объекты + фон»;
- изображение корреляционных пиков;
- гистограмму корреляционных пиков;
- выбранный порог;
- итоговое изображение обнаруженных объектов;
- число ложных обнаружений;
- число пропущенных объектов.

Результаты представить для $D_f=3000$ и $D_f=5000$.

### Вариант 2

- изображение фона;
- изображение объектов;
- результирующее изображение «объекты + фон»;
- результат корреляционного метода;
- результат разбеливания;
- результат согласованной фильтрации;
- гистограммы корреляционных пиков;
- выбранные пороги;
- итоговые изображения обнаруженных объектов;
- число ложных обнаружений;
- число пропущенных объектов.

Результаты удобно свести в таблицу:

| Тип объекта | Метод | Ложные обнаружения | Пропущенные объекты |
|---|---|---:|---:|
| Объект 1 | Корреляционный |  |  |
| Объект 1 | Согласованная фильтрация |  |  |
| Объект 2 | Корреляционный |  |  |
| Объект 2 | Согласованная фильтрация |  |  |

# 12. Выводы

В выводах необходимо объяснить полученные экспериментальные результаты.

Для варианта 1:

- как изменение дисперсии фона повлияло на качество обнаружения;
- как изменилось число ложных обнаружений;
- как изменилось число пропущенных объектов.

Для варианта 2:

- чем отличаются результаты корреляционного метода и согласованной фильтрации;
- как разбеливание фона повлияло на результат;
- для какого типа объектов методы дали различающиеся результаты;
- как изменилось число ложных и пропущенных объектов.

Выводы должны основываться на фактически полученных результатах.

# 13. Требования к реализации

Необходимо самостоятельно реализовать исследуемые методы.

Нельзя использовать готовые библиотечные реализации:

- корреляционного обнаружения объектов;
- template matching;
- согласованной фильтрации;
- готовых детекторов объектов.

Допускается использовать библиотеки для:

- работы с массивами;
- генерации случайных величин;
- визуализации изображений;
- построения гистограмм.

В коде должно быть явно видно:

- формирование изображения объектов;
- формирование фона;
- перемещение окна по изображению;
- вычисление отклика коррелятора;
- разбеливание фона для согласованной фильтрации;
- пороговая обработка;
- подсчёт ложных и пропущенных объектов.

# 14. Что сдавать

В репозитории должны быть:

- файл `.ipynb` с реализацией, включая:
  - результаты обязательных экспериментов;
  - изображения фона и объектов;
  - промежуточные изображения обработки;
  - гистограммы корреляционных пиков;
  - выбранные значения порогов;
  - итоговые изображения обнаруженных объектов;
  - количество ложных и пропущенных объектов;
  - краткие выводы.

# 15. Критерии зачёта

Лабораторная работа оценивается по системе зачёт/незачёт.

Для получения зачёта необходимо:

- полностью выполнить назначенный вариант;
- самостоятельно реализовать исследуемый метод обнаружения;
- провести все обязательные эксперименты;
- выполнить пороговую обработку;
- определить число ложных и пропущенных объектов;
- представить промежуточные и итоговые результаты;
- сделать краткие выводы;
- уметь объяснить собственную реализацию.

Если обязательная часть выполнена не полностью или реализация содержит существенные ошибки, работа получает **незачёт**.

# 16. Вопросы к защите

## 16.1. Теория

1. В чём заключается задача обнаружения объектов на изображении?

2. Что называется эталоном объекта?

3. Как работает корреляционный метод обнаружения?

4. Как определяется квадратичная мера различия?

5. Что показывает взаимная корреляция изображения и эталона?

6. Почему обычная взаимная корреляция может давать ошибочные результаты?

7. Для чего используется нормированная взаимная корреляция?

8. Как реализуется коррелятор с помощью линейной обработки «окном»?

9. Почему эталон при реализации коррелятора поворачивается на $180^\circ$?

10. Для чего после корреляционной обработки используется пороговая обработка?

11. Что такое корреляционный пик?

12. В чём состоит метод согласованной фильтрации?

13. Чем согласованная фильтрация отличается от обычного корреляционного метода?

14. Какие свойства фона учитывает согласованный фильтр?

15. Что такое разбеливающий фильтр?

16. Зачем выполняется разбеливание фона?

17. В каком случае согласованный фильтр становится обычным коррелятором?

18. Как задаётся биэкспоненциальная корреляционная функция фона?

19. Что характеризует коэффициент $\rho$?

20. Как реализуется согласованный фильтр для фона с биэкспоненциальной корреляционной функцией?

21. Что считается ложным обнаружением?

22. Что считается пропущенным объектом?

## 16.2. Вопросы по собственному коду

1. Как в коде формируется изображение фона?

2. Как задаются его дисперсия и коэффициент корреляции?

3. Как формируются объекты?

4. Как задаётся эталон объекта?

5. Как объекты размещаются на изображении?

6. Почему объекты не должны пересекаться?

7. Как формируется итоговое изображение из объектов и фона?

8. Где в коде реализован коррелятор?

9. Как вычисляется один элемент изображения корреляционных пиков?

10. Как обрабатываются границы изображения?

11. Как строится гистограмма корреляционных пиков?

12. Как выбирался порог?

13. Где выполняется пороговая обработка?

14. Как определяются координаты обнаруженных объектов?

15. Как считается число ложных обнаружений?

16. Как считается число пропущенных объектов?

17. Где в коде выполняется разбеливание фона?

18. Как формируется маска разбеливающего фильтра?

19. Как коэффициент $\rho$ влияет на значения $a_1$ и $a_2$?

20. Чем реализация согласованной фильтрации отличается от корреляционного метода?

21. Как изменение дисперсии фона повлияло на результаты вашего эксперимента?

22. Почему для разных типов объектов результаты могут отличаться?