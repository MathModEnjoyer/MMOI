# Лабораторная работа №5. Метод дифференциального кодирования изображений

## 1. Цель работы

Изучить методы сокращения избыточности изображений на основе дифференциальной импульсно-кодовой модуляции (ДИКМ).

В ходе лабораторной работы необходимо познакомиться с:

- основными показателями эффективности сжатия изображений;
- принципом дифференциального кодирования;
- предсказанием отсчётов изображения;
- формированием разностного сигнала;
- квантованием разностного сигнала;
- различными вариантами предсказателей;
- влиянием предсказателя на точность восстановления;
- помехоустойчивостью метода ДИКМ.

# 2. Теоретический минимум

## 2.1. Сжатие изображений

Цифровые изображения могут занимать значительный объём памяти.

Сжатие изображения — специальное кодирование, предназначенное для уменьшения объёма данных.

Основным показателем эффективности сжатия является коэффициент сжатия:

$$
K_{\text{сж}}
=
\frac{I_0}{I},
$$

где:

- $I_0$ — объём данных до сжатия;
- $I$ — объём данных после сжатия.

При

$$
K_{\text{сж}}>1
$$

объём данных после сжатия меньше исходного.

При сжатии с потерями восстановленное изображение может отличаться от исходного.

Для оценки точности используются максимальная и квадратичная погрешности.

## 2.2. Квадратичная погрешность

Пусть:

- $x(m,n)$ — исходное изображение;
- $\hat{x}(m,n)$ — восстановленное изображение;
- $M\times N$ — размер изображения.

Квадратичная погрешность определяется как

$$
\varepsilon_{\text{кв}}^2
=
\frac{1}{MN}
\sum_m
\sum_n
\left[
x(m,n)-\hat{x}(m,n)
\right]^2.
$$

Чем меньше значение квадратичной погрешности, тем ближе восстановленное изображение к исходному по данному критерию.

## 2.3. Максимальная погрешность

Максимальная погрешность определяется выражением

$$
\varepsilon_{\max}
=
\max_{m,n}
\left|
x(m,n)-\hat{x}(m,n)
\right|.
$$

Она характеризует максимальное отличие одного отсчёта восстановленного изображения от исходного.

# 3. Дифференциальная импульсно-кодовая модуляция

Основой метода ДИКМ является высокая корреляция между соседними отсчётами реального изображения.

При построчной обработке соседние значения яркости обычно отличаются незначительно.

Поэтому вместо самого значения

$$
x(m,n)
$$

кодируется его отличие от предсказанного значения.

Для каждого отсчёта вычисляется предсказание

$$
\hat{x}(m,n).
$$

Разностный сигнал определяется как

$$
f(m,n)
=
x(m,n)-\hat{x}(m,n).
$$

Если предсказание достаточно точное, значения $f(m,n)$ концентрируются около нуля.

Это позволяет использовать для их представления меньшее число бит.

# 4. Кодирование и декодирование

При компрессии для каждого отсчёта выполняются следующие действия:

1. вычисляется предсказание;
2. вычисляется разностное значение;
3. разностное значение квантуется;
4. квантованное значение кодируется.

При декомпрессии:

1. декодируется квантованное разностное значение;
2. вычисляется то же самое предсказание;
3. восстанавливается значение изображения.

Восстановленный отсчёт определяется как

$$
\hat{x}_{\text{восст}}(m,n)
=
f_q(m,n)
+
\hat{x}(m,n),
$$

где $f_q(m,n)$ — квантованный разностный сигнал.

При кодировании и декодировании должны использоваться одинаковые алгоритмы предсказания.

Предсказание при кодировании также должно выполняться по уже восстановленным отсчётам, поскольку при декодировании исходное изображение недоступно.

# 5. Коэффициент сжатия ДИКМ

Пусть:

- $b_0$ — число бит, используемое для представления исходного отсчёта;
- $b$ — число бит, используемое для представления квантованного разностного сигнала.

Тогда коэффициент сжатия определяется как

$$
K_{\text{сж}}
=
\frac{b_0}{b}.
$$

В лабораторной работе исследуются коэффициенты сжатия:

$$
K_{\text{сж}}=2,
$$

$$
K_{\text{сж}}=4,
$$

$$
K_{\text{сж}}=8.
$$

# 6. Предсказание отсчётов изображения

Точность предсказателя существенно влияет на эффективность ДИКМ.

Чем точнее предсказание, тем меньше дисперсия разностного сигнала и тем лучше этот сигнал подходит для последующего квантования и кодирования.

Линейный предсказатель может быть записан в виде

$$
\hat{x}(m,n)
=
\sum\sum_{(k,l)\in D}
g(k,l)x(m-k,n-l),
$$

где:

- $g(k,l)$ — коэффициенты предсказателя;
- $D$ — область используемых ранее обработанных отсчётов.

В лабораторной работе рассматривается предсказание по четырём соседним отсчётам:

$$
\hat{x}(m,n)
=
a_1x(m,n-1)
+
a_2x(m-1,n)
+
a_3x(m-1,n-1)
+
a_4x(m-1,n+1).
$$

Вектор коэффициентов обозначается

$$
A=(a_1,a_2,a_3,a_4).
$$

# 7. Исследуемые предсказатели

В работе необходимо исследовать четыре предсказателя.

### Предсказатель П-1

$$
A=(1,0,0,0).
$$

### Предсказатель П-2

$$
A=(0.5,0.5,0,0).
$$

### Предсказатель П-3

$$
A=(0.25,0.25,0.25,0.25).
$$

### Предсказатель П-4

$$
A=(1,1,-1,0).
$$

Основным недостатком линейных предсказателей является увеличение ошибки предсказания на участках со скачкообразным изменением яркости.

На восстановленном изображении это может проявляться в виде искажения границ и контуров.

# 8. Квантование разностного сигнала

В методе ДИКМ квантуется не исходное изображение, а разностный сигнал

$$
f(m,n).
$$

Область значений разностного сигнала разбивается на интервалы квантования.

Каждому интервалу соответствует некоторое квантованное значение.

Если используется $b$ бит, количество уровней квантования равно

$$
L=2^b.
$$

# 9. Равномерное квантование

При равномерном квантовании все интервалы имеют одинаковую ширину.

Для ДИКМ такой подход может быть неэффективен.

Если шкала слишком широкая:

- вероятность выхода сигнала за шкалу мала;
- расстояние между уровнями становится большим;
- возрастает погрешность квантования.

Если шкала слишком узкая:

- уменьшается расстояние между уровнями;
- увеличивается вероятность выхода разностного сигнала за диапазон шкалы;
- возникают перегрузки по наклону.

# 10. Неравномерное квантование

Для разностного сигнала целесообразно использовать неравномерную шкалу квантования.

Интервалы должны быть уже в областях, где вероятность появления значений выше.

Одной из таких шкал является шкала Макса.

Она строится из условия минимизации квадратичной погрешности квантования при заданном количестве уровней.

Квадратичная погрешность квантования определяется как

$$
\varepsilon_{\text{кв}}^2
=
\sum_{i=0}^{L-1}
\int_{d_i}^{d_{i+1}}
(f-f_i)^2W(f)\,df,
$$

где:

- $d_i$ — границы интервалов;
- $f_i$ — квантованные уровни;
- $W(f)$ — плотность распределения разностного сигнала.

Квантованный уровень определяется как

$$
f_i
=
\frac{
\displaystyle
\int_{d_i}^{d_{i+1}} fW(f)\,df
}{
\displaystyle
\int_{d_i}^{d_{i+1}} W(f)\,df
}.
$$

Граница между соседними уровнями определяется как

$$
d_i
=
\frac{f_{i-1}+f_i}{2}.
$$

# 11. Построение шкалы Макса

В общем случае шкала Макса строится итерационно.

1. В качестве начального приближения выбрать равномерную шкалу.

2. Пересчитать квантованные уровни $f_i$.

3. Пересчитать границы интервалов $d_i$.

4. Проверить изменение погрешности или положение уровней.

5. Если изменение меньше заданного значения — завершить построение.

6. В противном случае повторить пересчёт.

В данной лабораторной работе считается, что распределение вероятностей разностного сигнала имеет лапласовский вид.

# 12. Помехоустойчивость ДИКМ

Недостатком метода ДИКМ является низкая помехоустойчивость.

Предсказание текущего отсчёта зависит от ранее восстановленных отсчётов.

Поэтому ошибка в сжатых данных может повлиять не только на один отсчёт, но и распространиться на последующие отсчёты изображения.

В лабораторной работе ошибка передачи моделируется инвертированием одного бита в сжатых данных.

# 13. Выполнение лабораторной работы

Необходимо получить у преподавателя исследуемое изображение.

Для него необходимо выполнить два задания:

1. выбрать наиболее точный предсказатель;
2. исследовать помехоустойчивость метода ДИКМ.

Исследуемые алгоритмы необходимо реализовать самостоятельно.

# 14. Задание 1. Выбор наиболее точного предсказателя

Необходимо сравнить все четыре предсказателя:

- П-1;
- П-2;
- П-3;
- П-4.

Исследование проводится для коэффициентов сжатия

$$
K_{\text{сж}}=2,
$$

$$
K_{\text{сж}}=4,
$$

$$
K_{\text{сж}}=8.
$$

При построении квантователя считать, что распределение вероятностей разностного сигнала имеет лапласовский вид.

## 14.1. Порядок выполнения

Для каждого предсказателя:

1. Выполнить построчный обход изображения.

2. Для каждого отсчёта вычислить предсказанное значение.

3. Вычислить разностный сигнал

$$
f(m,n)
=
x(m,n)-\hat{x}(m,n).
$$

4. Выполнить квантование разностного сигнала.

5. Выполнить кодирование.

6. Выполнить декодирование.

7. Восстановить изображение.

8. Рассчитать погрешность восстановления.

9. Выполнить эксперимент для

$$
K_{\text{сж}}=2.
$$

10. Повторить эксперимент для

$$
K_{\text{сж}}=4.
$$

11. Повторить эксперимент для

$$
K_{\text{сж}}=8.
$$

12. Сравнить результаты четырёх предсказателей.

13. Выбрать наиболее точный предсказатель.

14. Обосновать выбор на основании результатов эксперимента.

# 15. Эксперимент 1. Точность предсказателей

Результаты удобно представить в таблице.

| Предсказатель | $K_{\text{сж}}=2$ | $K_{\text{сж}}=4$ | $K_{\text{сж}}=8$ |
|---|---:|---:|---:|
| П-1 |  |  |  |
| П-2 |  |  |  |
| П-3 |  |  |  |
| П-4 |  |  |  |

В ячейках таблицы привести значение выбранной погрешности восстановления.

На основании результатов определить наиболее точный предсказатель.

Необходимо объяснить полученный результат, а не заранее предполагать, какой из предсказателей должен оказаться лучшим.

# 16. Задание 2. Исследование помехоустойчивости

Исследование проводится при

$$
K_{\text{сж}}=8.
$$

Необходимо исследовать каждый из четырёх предсказателей.

Ошибка передачи моделируется инвертированием одного бита сжатых данных.

## 16.1. Порядок выполнения

Для каждого предсказателя:

1. Выполнить сжатие изображения.

2. Получить восстановленное изображение без ошибки передачи.

3. Инвертировать один бит, расположенный в первой четверти сжатых данных.

4. Выполнить декодирование повреждённых данных.

5. Получить восстановленное изображение после сбоя.

6. Вычислить изображение погрешности между:
   - восстановленным изображением без сбоя;
   - восстановленным изображением после сбоя.

7. Контрастировать изображение погрешности и вывести его.

8. Повторить эксперимент, расположив ошибочный бит во второй четверти сжатых данных.

9. Повторить эксперимент для третьей четверти.

10. Повторить эксперимент для четвёртой четверти.

11. Визуально классифицировать полученные изображения погрешностей.

12. Объяснить характер распространения ошибки.

13. Повторить исследование для всех четырёх предсказателей.

14. Определить наиболее помехоустойчивый предсказатель.

15. Обосновать сделанный выбор.

# 17. Эксперимент 2. Положение ошибки в сжатых данных

Для каждого предсказателя необходимо получить четыре результата:

- ошибка в первой четверти файла;
- ошибка во второй четверти файла;
- ошибка в третьей четверти файла;
- ошибка в четвёртой четверти файла.

Для каждого случая представить:

- восстановленное изображение после сбоя;
- контрастированное изображение погрешности.

Необходимо объяснить:

- как распространяется ошибка;
- от какой части изображения начинается искажение;
- зависит ли характер распространения ошибки от положения повреждённого бита;
- зависит ли результат от выбранного предсказателя.

# 18. Результаты

## 18.1. Для задания 1

Необходимо представить:

- исходное изображение;
- восстановленные изображения для исследованных предсказателей;
- значения погрешности при $K_{\text{сж}}=2$;
- значения погрешности при $K_{\text{сж}}=4$;
- значения погрешности при $K_{\text{сж}}=8$;
- сравнительную таблицу;
- выбранный наиболее точный предсказатель;
- обоснование выбора.

## 18.2. Для задания 2

Необходимо представить:

- восстановленное изображение без ошибки;
- восстановленные изображения после инвертирования бита;
- контрастированные изображения погрешностей;
- результаты для каждой четверти сжатых данных;
- результаты для каждого предсказателя;
- выбранный наиболее помехоустойчивый предсказатель;
- обоснование выбора.

# 19. Выводы

В выводах необходимо описать результаты проведённых экспериментов.

Для первого задания:

- как выбор предсказателя влияет на качество восстановления;
- как меняется погрешность при изменении коэффициента сжатия;
- какой предсказатель оказался наиболее точным;
- чем можно объяснить полученный результат.

Для второго задания:

- как единичная ошибка в сжатых данных влияет на восстановленное изображение;
- как положение ошибочного бита влияет на результат;
- чем отличаются результаты для разных предсказателей;
- какой предсказатель оказался наиболее помехоустойчивым.

Выводы должны основываться на фактически полученных результатах.

# 20. Требования к реализации

Необходимо самостоятельно реализовать основные этапы ДИКМ:

- построчный обход изображения;
- предсказание отсчётов;
- формирование разностного сигнала;
- квантование;
- кодирование;
- декодирование;
- восстановление изображения;
- моделирование ошибки передачи;
- вычисление изображений погрешности.

Нельзя использовать готовую реализацию ДИКМ.

Допускается использование библиотек для:

- загрузки и отображения изображений;
- работы с массивами;
- построения графиков;
- вычисления вспомогательных статистических характеристик.

В коде должно быть явно видно:

- какой предсказатель используется;
- как формируется предсказанное значение;
- как получается разностный сигнал;
- как выполняется квантование;
- как восстанавливаются отсчёты изображения;
- где моделируется инвертирование бита;
- как рассчитывается погрешность.

# 21. Что сдавать

В репозитории должны быть:

- файл `.ipynb` с реализацией, включая:
  - результаты обязательных экспериментов;
  - исходное и восстановленные изображения;
  - результаты для коэффициентов сжатия 2, 4 и 8;
  - сравнительную таблицу предсказателей;
  - выбранный наиболее точный предсказатель;
  - результаты экспериментов с инвертированием бита;
  - контрастированные изображения погрешностей;
  - выбранный наиболее помехоустойчивый предсказатель;
  - краткие выводы.

# 22. Критерии зачёта

Лабораторная работа оценивается по системе зачёт/незачёт.

Для получения зачёта необходимо:

- выполнить оба обязательных задания;
- реализовать ДИКМ;
- исследовать все четыре предсказателя;
- провести эксперименты при коэффициентах сжатия 2, 4 и 8;
- выбрать и обосновать наиболее точный предсказатель;
- провести четыре эксперимента со сбоем для каждого предсказателя;
- получить изображения погрешностей;
- выбрать и обосновать наиболее помехоустойчивый предсказатель;
- сделать краткие выводы;
- уметь объяснить собственную реализацию.

Если обязательная часть выполнена не полностью или реализация содержит существенные ошибки, работа получает **незачёт**.

# 23. Вопросы к защите

## 23.1. Теория

1. Зачем выполняется сжатие изображений?

2. Что называется коэффициентом сжатия?

3. Какие показатели используются для оценки точности сжатия?

4. Как определяется квадратичная погрешность?

5. Как определяется максимальная погрешность?

6. На каком свойстве реальных изображений основан метод ДИКМ?

7. Что такое разностный сигнал?

8. Почему разностный сигнал можно представить меньшим числом бит?

9. Из каких этапов состоит кодирование методом ДИКМ?

10. Из каких этапов состоит декодирование?

11. Почему при кодировании и декодировании должен использоваться одинаковый предсказатель?

12. Почему при кодировании предсказание должно выполняться по восстановленным отсчётам?

13. Как коэффициент сжатия связан с количеством бит?

14. От чего зависит эффективность предсказателя?

15. Как формируется линейное предсказание?

16. Какие четыре предсказателя исследуются в работе?

17. Как линейные предсказатели работают в области резкого изменения яркости?

18. Что такое квантование?

19. Что называется шкалой квантования?

20. Почему равномерная шкала может плохо подходить для ДИКМ?

21. Что происходит при слишком широкой шкале?

22. Что происходит при слишком узкой шкале?

23. Что такое перегрузка по наклону?

24. В чём состоит идея неравномерного квантования?

25. Что такое шкала Макса?

26. По какому критерию строится шкала Макса?

27. Как строится шкала Макса итерационно?

28. Почему метод ДИКМ обладает низкой помехоустойчивостью?

29. Почему ошибка одного значения может распространяться на последующие отсчёты?

## 23.2. Вопросы по собственному коду

1. Как реализован построчный обход изображения?

2. Где в коде выбирается предсказатель?

3. Какие четыре соседних отсчёта используются для предсказания?

4. Как вычисляется предсказанное значение?

5. Как получается разностный сигнал?

6. Как в коде реализовано квантование?

7. Как количество уровней квантования связано с коэффициентом сжатия?

8. Как выполняется кодирование квантованного сигнала?

9. Как выполняется декодирование?

10. Как восстанавливается очередной отсчёт изображения?

11. Почему при восстановлении нельзя использовать исходное изображение?

12. Как рассчитывается квадратичная погрешность?

13. Какой предсказатель оказался наиболее точным?

14. Чем вы объясняете этот результат?

15. Как изменение $K_{\text{сж}}$ повлияло на погрешность?

16. Где в коде выполняется инвертирование бита?

17. Как выбирается бит из первой, второй, третьей или четвёртой четверти данных?

18. Как строится изображение погрешности после сбоя?

19. Зачем изображение погрешности контрастируется?

20. Как распространялась ошибка после повреждённого бита?

21. Почему результаты отличаются для разных положений сбоя?

22. Какой предсказатель оказался наиболее помехоустойчивым?

23. Чем вы объясняете его поведение?